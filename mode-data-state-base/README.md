# About mode-data-state-base Module

# Requirements
This module is only to be used with React apps. This module does not provide any UI but it does depends on some of React's hooks such as useContext
and useReducer.

